This project classifies dolphin vocalizations into three categories:

    Click

    Whistle

    None

The system accepts a WAV audio file as input, converts it into a spectrogram, and performs classification using a pre‑trained CNN model deployed as a web service.